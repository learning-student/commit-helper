{

    "version": "0.2.0",
    "configurations": [
        {
            "args": [
                "changelog", "--dir", "../invitation-api"
            ],
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal"
                }
    ]
}